# PostgreSQL is recommended for shared or production environments.
DATABASE_URL=postgresql+psycopg://qualisphere:change-me@localhost:5432/qualisphere
ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Configure only in an approved deployment. Never commit this value.
GROQ_API_KEY=
